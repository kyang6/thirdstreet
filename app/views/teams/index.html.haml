.container
	%table
		%thead
			%tr
				%th
	      
		%tbody
			- @teams.each do |team|
				=link_to image_tag(team.image.url(:medium),class:"title_center"), team
	     
	%br
-if user_signed_in?
	= link_to 'New Team', new_team_path
